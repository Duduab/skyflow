/** Station 6 — כמה תמונות תיעוד נדרשות (ברירת מחדל: 3) */
export function packPhotoRequiredCount(_totalItems: number): number {
  return 3;
}

/** מקסימום משבצות (נדרשות + אופציונליות) */
export const MAX_PACK_PHOTO_SLOTS = 24;
